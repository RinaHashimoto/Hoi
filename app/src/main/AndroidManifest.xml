<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rina.hoi">

    <application
        android:name=".Globals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".startActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".jankenActivity" />
        <activity android:name=".jankenResultActivity" />
        <activity android:name=".selectTimesActivity" />
        <activity android:name=".hoiActivity" />
        <activity android:name=".hoiResultActivity" />
        <activity android:name=".resultActivity"></activity>
    </application>

</manifest>